from setuptools import setup, find_packages

setup(
    name='timer_gen',
    version='1.0.0',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
        # list your dependencies here if you have any
    ],
    py_modules=['timer_gen'],
)